export default async function handler(_req,res){const keys={fmp:'FMP_API_KEY',twelve:'TWELVE_DATA_API_KEY',alpha:'ALPHA_VANTAGE_API_KEY',groq:'GROQ_API_KEY',news:'NEWS_API_KEY'};const configured=Object.fromEntries(Object.entries(keys).map(([name,key])=>[name,Boolean(process.env[key])]));res.status(200).json({ok:true,configured,warning:'Configured only means a key exists. Provider permissions, quotas, symbol coverage and freshness must be tested per request.',checkedAt:new Date().toISOString()})}
