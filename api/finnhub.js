// Finnhub adapter for supported global symbols. Server-side key only.
export default async function handler(req,res){const symbol=String(req.query?.symbol||'').trim().toUpperCase();if(!symbol)return res.status(400).json({error:'symbol is required'});const key=process.env.FINNHUB_API_KEY;if(!key)return res.status(503).json({error:'FINNHUB_API_KEY is not configured'});try{const r=await fetch(`https://finnhub.io/api/v1/quote?symbol=${encodeURIComponent(symbol)}&token=${encodeURIComponent(key)}`);const d=await r.json();if(!r.ok||!d?.c)return res.status(r.status||404).json({error:'Finnhub returned no verified quote',details:d});return res.status(200).json({mode:'verified',quoteVerified:true,symbol,price:d.c,change:d.d,changePercent:d.dp,dayHigh:d.h,dayLow:d.l,open:d.o,previousClose:d.pc,source:'Finnhub',provider:'finnhub',updatedAt:new Date(d.t*1000).toISOString()})}catch(e){return res.status(502).json({error:'Finnhub unavailable',details:e.message})}}
